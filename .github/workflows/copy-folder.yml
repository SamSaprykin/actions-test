name: Copy Posts

on: push

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Pushes to another repository
        id: push_directory
        uses: SamSaprykin/actions-test@master
        env:
          API_TOKEN_GITHUB: ${{ secrets.API_TOKEN_GITHUB }}
        with:
          source_folder: 'guides'
          destination_repo: 'SamSaprykin/dest-actions'
          destination_folder: 'static'
          user-email: saprykinse@gmail.com
          user-name: SamSaprykin
          commit-message: Update data with the latest published blog posts
          target-branch: main